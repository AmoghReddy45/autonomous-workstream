# Runtime artifacts of autonomous runs (audit log + Q&A inbox/outbox + lessons log).
# These are produced inside a *consuming* project, not this repo, but ignore them
# here too so demo/smoke-test runs never get committed.
data/automation/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
